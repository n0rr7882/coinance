import React from 'react';
import UserSettingDialog from '../../components/UserSettingDialog';

export default class UserSettingDialogContainer extends React.Component<{}> {
  render() {
    return (
      <UserSettingDialog />
    );
  }
}
